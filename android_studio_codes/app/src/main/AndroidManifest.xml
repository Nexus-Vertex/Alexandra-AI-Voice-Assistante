<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapp">

    <!-- Internet pour Firebase -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Permissions pour Alexandra -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>

    <!-- Permission pour lister les apps installées -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"/>

    <!-- Permission pour choisir une photo depuis la galerie -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.FLASHLIGHT"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- Alarme -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>

    <!-- ✅ NOUVEAU — Rappels / Notifications programmées -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- ✅ Queries WhatsApp normal + Business -->
    <queries>
        <package android:name="com.google.android.googlequicksearchbox"/>
        <package android:name="com.whatsapp"/>
        <package android:name="com.whatsapp.w4b"/>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Alexandra">

        <!-- Écran de démarrage -->
        <activity
            android:name=".IntroActivity"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Connexion -->
        <activity
            android:name=".RappelsActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Inscription -->
        <activity
            android:name=".RegisterActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Mot de passe oublié -->
        <activity
            android:name=".ForgotPasswordActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Écran principal / Alexandra -->
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Historique des commandes -->
        <activity
            android:name=".HistoriqueActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Profil utilisateur -->
        <activity
            android:name=".ProfilActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Autorisations -->
        <activity
            android:name=".AutorisationsActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Applications connectées -->
        <activity
            android:name=".AppsConnecteesActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Modifier nom et email -->
        <activity
            android:name=".EditNomEmailActivity"
            android:screenOrientation="portrait"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <!-- Changer mot de passe -->
        <activity
            android:name=".ChangePasswordActivity"
            android:screenOrientation="portrait"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <!-- Notifications -->
        <activity
            android:name=".NotificationsActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- ✅ Accessibility Service pour WhatsApp -->
        <service
            android:name=".AlexandraAccessibilityService"
            android:exported="true"
            android:label="Alexandra - Assistant vocal"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config"/>
        </service>

        <!-- Notification Listener -->
        <service android:name=".AlexandraNotificationService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService"/>
            </intent-filter>
        </service>

        <!-- ✅ NOUVEAU — Receiver pour les rappels Alexandra -->
        <receiver
            android:name=".RappelReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>